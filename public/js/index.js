// index.js

import { Loader, Popin, Notify, Headband, Colors, Charts, Window } from "./_common.js";

$(document).ready(() => {

    // Loader.Show('loader', 'body')
    // setTimeout(() => {
    //     Loader.Hide('#loader')
    // }, 2000)

    Window.Show(
        'window', 
        'body',
        '/settings'
    )

})